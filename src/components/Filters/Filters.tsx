import DropdownButton from "./DropdownButton";

function Filters({ movies, genre }) {
	return (
		<>
			<div>
				<DropdownButton />
				<DropdownButton />
				<DropdownButton />
				<DropdownButton />
			</div>
		</>
	);
}

export default Filters;
